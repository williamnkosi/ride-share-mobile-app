import { StyleSheet, Text, View } from "react-native";
import React from "react";

type Props = {};

const RideOptionsCard = (props: Props) => {
  return (
    <View>
      <Text>RideOptionsCard</Text>
    </View>
  );
};

export default RideOptionsCard;

const styles = StyleSheet.create({});
